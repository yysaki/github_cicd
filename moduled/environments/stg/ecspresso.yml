region: ap-northeast-1
cluster: stg-example-cluster
service: stg-example-service
service_definition: ecs-service-def.jsonnet
task_definition: ecs-task-def.jsonnet
timeout: "10m0s"
required_version: ">= 2.0.0, < 3"

plugins:
  - name: tfstate
    config:
      url: "s3://{{ must_env `TF_VAR_tfstate_s3_bucket` }}/moduled/stg/terraform.tfstate"
